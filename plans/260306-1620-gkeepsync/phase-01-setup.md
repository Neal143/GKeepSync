# Phase 01: Setup & Dependencies
Status: ⬜ Pending
Dependencies: None

## Objective
Khởi tạo project Python, cài dependencies, tạo cấu trúc thư mục.

## Implementation Steps
1. [ ] Tạo virtual environment
2. [ ] Tạo `requirements.txt` với các packages:
   - `gkeepapi` — Google Keep unofficial API
   - `gpsoauth` — Lấy Master Token
   - `customtkinter` — Modern GUI framework
   - `pyinstaller` — Build .exe
3. [ ] Install dependencies
4. [ ] Tạo cấu trúc thư mục (main.py, ui/, utils/)
5. [ ] Tạo `config.py` — quản lý config file JSON
6. [ ] Test: Import tất cả packages thành công

## Files to Create
- `requirements.txt`
- `main.py` (entry point skeleton)
- `config.py`
- `ui/__init__.py`
- `utils/__init__.py`

## Test Criteria
- [ ] Virtual environment hoạt động
- [ ] Tất cả packages import được
- [ ] `python main.py` chạy không lỗi (dù chưa có UI)
